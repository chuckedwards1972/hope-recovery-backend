const{Pool}=require("pg");const p=new Pool({connectionString:"postgresql://postgres:KsKWWdFSEUAiWhrwrThNtnyIMCLUKAVH@hopper.proxy.rlwy.net:52331/railway",ssl:{rejectUnauthorized:false}});const sql="CREATE TABLE IF NOT EXISTS rrn_licensing_partners (id SERIAL PRIMARY KEY, org_name TEXT NOT NULL, contact TEXT, email TEXT, phone TEXT, org_type TEXT, member_count INTEGER DEFAULT 0, source TEXT, stage TEXT DEFAULT $$inquiry$$, notes TEXT, monthly_fee NUMERIC(10,2), contract_start DATE, contract_end DATE, created_by INTEGER, created_at TIMESTAMPTZ DEFAULT NOW(), updated_at TIMESTAMPTZ DEFAULT NOW()); CREATE INDEX IF NOT EXISTS idx_licensing_stage ON rrn_licensing_partners(stage);";p.query(sql).then(()=>{console.log("Done");p.end()}).catch(e=>{console.error("FAILED:",e.message);p.end()})
